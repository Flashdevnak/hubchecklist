# Android Native Plugin Notes

Future MVP-006 will add `FlashProofWebViewPlugin` here after the Capacitor Android project is generated.

Required responsibilities:

- Open only `api.flashexpress.com` URLs in controlled WebView.
- Fill driver phone number with JavaScript injection in WebView.
- Click the Thai `ค้นหา` button.
- Extract Flash route/status table by resilient Thai label matching.
- Return structured JSON to React.
- Capture screenshot or HTML snapshot for audit.

This folder is intentionally not a real plugin yet in MVP-001.
