import PlaceholderPage from '../components/PlaceholderPage';

export default function FlashSearchPage() {
  return (
    <PlaceholderPage
      route="flash-search"

      sample={
        <p className="muted-note">Android WebView auto-fill phone จะเพิ่มใน MVP-006</p>
      }
    />
  );
}
