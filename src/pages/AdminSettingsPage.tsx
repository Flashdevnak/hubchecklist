import PlaceholderPage from '../components/PlaceholderPage';

export default function AdminSettingsPage() {
  return (
    <PlaceholderPage
      route="admin-settings"

      sample={
        <p className="muted-note">ตั้งค่า threshold และ photo rules จะเพิ่มภายหลัง</p>
      }
    />
  );
}
