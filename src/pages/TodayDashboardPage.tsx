import PlaceholderPage from '../components/PlaceholderPage';

export default function TodayDashboardPage() {
  return (
    <PlaceholderPage
      route="dashboard"

      sample={
        <p className="muted-note">Dashboard จะนับจาก vehicle_records ที่สร้างจากสแกนจริง</p>
      }
    />
  );
}
