import PlaceholderPage from '../components/PlaceholderPage';

export default function UserManagementPage() {
  return (
    <PlaceholderPage
      route="user-management"

      sample={
        <p className="muted-note">จัดการ user/role/branch จะเพิ่มภายหลัง</p>
      }
    />
  );
}
