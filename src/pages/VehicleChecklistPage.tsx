import PlaceholderPage from '../components/PlaceholderPage';

export default function VehicleChecklistPage() {
  return (
    <PlaceholderPage
      route="checklist"

      sample={
        <p className="muted-note">รูปที่ต้องถ่ายจะแสดงตาม checklistType</p>
      }
    />
  );
}
